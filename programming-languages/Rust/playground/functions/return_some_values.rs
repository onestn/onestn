fn _some_func(_a: String) -> String {
    "test".to_string()
}

fn add_values(num_1: i32, num_2: i32) -> i32 {
    let added_value: i32 = num_1 + num_2; 

    added_value
}

fn main() {
    // println!(some_fu("Hello".to_string());

    println!("{}", add_values(100, 120));
}
